spring.application.name=backend

# ===================================================================
# In-Memory Mode Configuration
# -------------------------------------------------------------------
# Currently, this application runs in an in-memory mode.
# Test data is hardcoded in TestService.java.
#
# Database connection properties (e.g., for PostgreSQL) will be added
# in a future step and managed via profiles (e.g., application-prod.yml).
# ===================================================================